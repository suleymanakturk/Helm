apiVersion: v1
kind: Service
metadata:
  labels:
    app: python-app
  name: python-app
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: python-app
  type: LoadBalancer
